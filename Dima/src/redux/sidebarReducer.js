
let initialState = {
    usersOnline: [
        {id: 1, name: 'Dima'},
        {id: 2, name: 'Ajnura'},
        {id: 3, name: 'Kirill'}
    ]
}

const sidebarReducer = (state = initialState, action) => {

    return state;
}

export default sidebarReducer;